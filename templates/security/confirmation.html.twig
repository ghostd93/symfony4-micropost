{% extends 'base.html.twig' %}

{% block body %}
    {% if user %}
        <p>Your account is confirmed! You can now sign in </p><a href="{{ path("app_login") }}">here</a>
    {% else %}
        <p>Something went wrong.</p>
    {% endif %}
{% endblock %}